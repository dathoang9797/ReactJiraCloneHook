import React from 'react';
import { PropsPage } from '@models/Global.types';
import Header from '@layouts/Header';

const Detail = (props: PropsPage) => {
  return (
    <div>
      <Header />
      Detail
    </div>
  );
};

export default Detail;
