import React from 'react';
import { PropsPage } from '@models/Global.types';
import Header from '@layouts/Header';
const Profile = (props: PropsPage) => {
  return (
    <div>
      <Header />
      Profile
    </div>
  );
};

export default Profile;
