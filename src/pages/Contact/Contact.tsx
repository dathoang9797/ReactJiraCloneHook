import React from 'react';
import { PropsPage } from '@models/Global.types';
import Header from '@layouts/Header';

const Contact = (props: PropsPage) => {
  return (
    <div>
      <Header />
      Contact
    </div>
  );
};

export default Contact;
