import React from 'react';
import Header from '@layouts/Header';
import { PropsPage } from '@models/Global.types';

const PageNotFound = (props: PropsPage) => {
  return (
    <div>
      {' '}
      <Header /> PageNotFound
    </div>
  );
};

export default PageNotFound;
